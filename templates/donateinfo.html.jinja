<!DOCTYPE html>
<html lang="en" id="htmlRoot">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FridgeNet - Community Support</title>
    <link rel="icon" type="image/png" href="/static/images/logo.png">

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = { darkMode: 'class' }
    </script>

    <script>
        (function () {
            const saved = localStorage.getItem("theme");
            if (saved === "dark") document.documentElement.classList.add("dark");
            else if (saved === "light") document.documentElement.classList.remove("dark");
            else {
                const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
                document.documentElement.classList.toggle("dark", prefersDark);
            }
        })();
    </script>

    <style>
        .input {
            width: 100%; padding: 0.75rem; border-radius: 0.75rem;
            border: 1px solid #d1d5db; background: #f8fafc; transition: 0.25s;
            font-size: 16px; /* Prevents iOS auto-zoom on focus */
        }
        .input:focus { border-color: #a78bfa; box-shadow: 0 0 0 3px rgba(167, 139, 250, 0.3); outline: none; }
        .form-card { background: linear-gradient(135deg, #ffffff, #f3f0ff); border: 1px solid #cac2d2; }
        .dark .form-card { background: linear-gradient(135deg, #1e1b2e, #2a2545); border: 1px solid #40376E; }
        .dark .input { background: #2a2545; border-color: #40376E; color: #fff; }
        .dropdown { background: white; }
        .dark .dropdown { background: #2a2545; border-color: #40376E; }
        html { scroll-behavior: smooth; transition: background-color 0.3s ease, color 0.3s ease; }
    </style>
</head>

<body class="bg-white text-gray-800 dark:bg-[#1b1730] dark:text-gray-100 transition-colors duration-300">

{% include "components/navbar.html.jinja" %}

{% with messages = get_flashed_messages(with_categories=true) %}
  {% if messages %}
    {% for category, message in messages %}
      {% if category == "donate_money" %}
        <div class="flash-message mb-4 px-4 py-3 rounded-xl 
                    bg-green-600 dark:bg-[#6f52c9] text-white text-sm shadow-md fixed top-24 md:top-20 right-5 z-50 space-y-2 ">
          {{ message }}
        </div>
      {% endif %}
    {% endfor %}
  {% endif %}
{% endwith %}

<div class="max-w-6xl mx-auto px-4 sm:px-6 py-6 lg:py-12">

    <div class="mb-8 lg:mb-12 text-center lg:text-left">
        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold tracking-tight">Help People In Need</h1>
        <p class="text-gray-600 dark:text-gray-300 mt-3 text-base sm:text-lg">Donate money or food to support local communities.</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-10">

        <div class="form-card rounded-3xl shadow-xl p-5 sm:p-8 flex flex-col justify-between">
            <div>
                <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
                   
                    Donate Money
                </h2>

                <form method="POST" action="{{ url_for('donate_money') }}" id="moneyForm"
                 onsubmit="console.log('SUBMITTING FORM')">
                    <div class="grid grid-cols-3 gap-3 sm:gap-4 font-semibold">
                        {% for amount in [10, 25, 50] %}
                        <label class="cursor-pointer group">
                            <input type="radio" name="amount" value="{{ amount }}" class="hidden peer">
                            <div class="py-3 sm:py-4 text-center rounded-2xl border border-gray-200 dark:border-gray-700 peer-checked:bg-orange-600 peer-checked:border-orange-600 hover:bg-orange-50 peer-checked:text-white dark:peer-checked:bg-purple-600 dark:peer-checked:border-purple-600 transition-all duration-200">
                                ${{ amount }}
                            </div>
                        </label>
                        {% endfor %}
                    </div>

                    <div class="relative">
                        <input type="number" name="custom_amount" id="customAmount" placeholder="$ Custom amount" class="input" min="1" max="10000">
                    </div>

                    <div class="relative space-y-1.5">
                        <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Target Fridge</label>
                        <input type="hidden" name="money_fridge_id" id="moneyFridgeValue" required>
                        <button type="button" id="moneyFridgeBtn" class="w-full p-4 rounded-xl border bg-white dark:bg-[#2a2545] dark:text-white flex justify-between items-center text-sm transition shadow-sm">
                            <span id="moneyFridgeText">Choose a fridge</span>
                            <span class="text-xs opacity-50">▼</span>
                        </button>
                        <div id="moneyFridgeMenu" class="dropdown absolute z-30 w-full mt-2 max-h-60 rounded-xl shadow-2xl border overflow-y-auto hidden">
                            {% for fridge in fridges %}
                            <div class="money-item flex items-center gap-3 p-4 cursor-pointer hover:bg-orange-50 dark:hover:bg-[#40376E] border-b last:border-0 border-gray-100 dark:border-gray-700/50"
                                data-id="{{ fridge['ID'] }}" data-name="{{ fridge['Name'] }}">
                                <img src="{{ fridge['Image'] }}" class="w-10 h-10 rounded-lg object-cover">
                                <span class="text-sm font-bold">{{ fridge['Name'] }}</span>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                </form>
            </div>
            <button type="submit" form="moneyForm" class="w-full mt-8 bg-orange-600 hover:bg-orange-700 font-bold text-white py-4 rounded-2xl dark:bg-purple-600 dark:hover:bg-purple-700 shadow-lg shadow-orange-600/20 dark:shadow-purple-900/20 transition-all transform active:scale-[0.98]">
                Donate Now
            </button>
        </div>

        <div class="form-card rounded-3xl shadow-xl p-5 sm:p-8">
            <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
                Schedule Drop-Off
            </h2>

            <form method="POST" action="{{ url_for('donate_food') }}" class="space-y-4">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Name</label>
                        <input type="text" name="full_name" placeholder="Full Name" class="input" required>
                    </div>
                    <div class="space-y-1">
                        <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Email</label>
                        <input type="email" name="food_email" placeholder="Email" class="input" required>
                    </div>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Dropoff Date</label>
                        <input type="date" name="dropoff_date" id="dropoffDate" class="input" required>
                    </div>
                    <div class="space-y-1">
                        <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Quantity</label>
                        <input type="number" name="quantity" placeholder="No. of items" class="input" required>
                    </div>
                </div>

                <div class="space-y-1 relative">
                    <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Type of Food</label>
                    <input type="hidden" name="food_type" id="foodTypeValue" required>
                    <button type="button" id="foodTypeBtn" class="w-full p-4 rounded-xl border bg-white dark:bg-[#2a2545] border-gray-200 dark:border-gray-700 dark:text-white flex justify-between items-center text-sm shadow-sm transition-colors">
                        <span id="foodTypeText">Select category</span>
                        <span class="text-xs opacity-50">▼</span>
                    </button>
                    <div id="foodTypeMenu" class="absolute z-20 w-full mt-2 max-h-60 rounded-xl shadow-2xl border bg-white dark:bg-[#2a2545] border-gray-100 dark:border-gray-700 overflow-y-auto hidden">
                        {% for item in food_types %}
                        <div class="food-type-item p-4 cursor-pointer hover:bg-orange-50 dark:hover:bg-[#40376E] border-b last:border-0 border-gray-100 dark:border-gray-700/50 transition-colors"
                            data-id="{{ item['ID'] }}" data-name="{{ item['Name'] }}">
                            <span class="text-sm font-bold">{{ item['Name'] }}</span>
                        </div>
                        {% endfor %}
                    </div>
                </div>

                <div class="space-y-1 relative">
                    <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Target Fridge</label>
                    <input type="hidden" name="FridgeID" id="foodFridgeValue" required>
                    <button type="button" id="foodFridgeBtn" class="w-full p-4 rounded-xl border bg-white dark:bg-[#2a2545] dark:text-white flex justify-between items-center text-sm shadow-sm">
                        <span id="foodFridgeText">Choose a location</span>
                        <span class="text-xs opacity-50">▼</span>
                    </button>
                    <div id="foodFridgeMenu" class="dropdown absolute z-20 w-full mt-2 max-h-60 rounded-xl shadow-2xl border overflow-y-auto hidden">
                        {% for fridge in fridges %}
                        <div class="food-item flex items-center gap-3 p-4 cursor-pointer hover:bg-orange-50 dark:hover:bg-[#40376E] border-b last:border-0 border-gray-100 dark:border-gray-700/50"
                            data-id="{{ fridge['ID'] }}" data-name="{{ fridge['Name'] }}">
                            <img src="{{ fridge['Image'] }}" class="w-10 h-10 rounded-lg object-cover">
                            <span class="text-sm font-bold">{{ fridge['Name'] }}</span>
                        </div>
                        {% endfor %}
                    </div>
                </div>
            </label>
           
       

                <div class="space-y-1">
                    <label class="block text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 ml-1">Notes</label>
                    <textarea name="notes" rows="2" class="input" placeholder="Any dietary info or specifics?"></textarea>
                </div>

                <button type="submit" class="w-full bg-orange-600 font-bold hover:bg-orange-700 text-white py-4 rounded-2xl dark:bg-purple-600 dark:hover:bg-purple-700 transition transform active:scale-[0.98] mt-4 shadow-lg">
                    Complete Donation
                </button>
            </form>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function () {
    // Dropdown Logic
    function setupDropdown(btnId, menuId, hiddenInputId, textId, itemClass) {
        const btn = document.getElementById(btnId);
        const menu = document.getElementById(menuId);
        const hiddenInput = document.getElementById(hiddenInputId);
        const btnText = document.getElementById(textId);

        if (!btn || !menu) return;

        btn.addEventListener('click', (e) => {
            e.stopPropagation();
            document.querySelectorAll('.dropdown, #foodTypeMenu').forEach(m => {
                if(m !== menu) m.classList.add('hidden');
            });
            menu.classList.toggle('hidden');
        });

        menu.querySelectorAll(itemClass).forEach(item => {
            item.addEventListener('click', function () {
                hiddenInput.value = this.getAttribute('data-id');
                btnText.innerText = this.getAttribute('data-name');
                menu.classList.add('hidden');
                btn.classList.add('border-orange-500', 'dark:border-purple-500');
            });
        });
    }

    setupDropdown('moneyFridgeBtn', 'moneyFridgeMenu', 'moneyFridgeValue', 'moneyFridgeText', '.money-item');
    setupDropdown('foodFridgeBtn', 'foodFridgeMenu', 'foodFridgeValue', 'foodFridgeText', '.food-item');
    setupDropdown('foodTypeBtn', 'foodTypeMenu', 'foodTypeValue', 'foodTypeText', '.food-type-item');

    // Close on outside click
    document.addEventListener('click', () => {
        document.querySelectorAll('.dropdown, #foodTypeMenu').forEach(m => m.classList.add('hidden'));
    });

    // Amount Toggle Logic
    const customInput = document.getElementById("customAmount");
    const presetRadios = document.querySelectorAll('input[name="amount"]');

    customInput?.addEventListener("input", () => {
        if (customInput.value !== "") {
            presetRadios.forEach(r => r.checked = false);
        }
    });

    presetRadios.forEach(radio => {
        radio.addEventListener("change", () => { 
            if(radio.checked) customInput.value = ""; 
        });
    });

    // Form Validation & Visual State
    const dateInput = document.getElementById("dropoffDate");
    if (dateInput) {
        const today = new Date().toISOString().split("T")[0];
        dateInput.min = today;
    }
    
    const flashes = document.querySelectorAll(".flash-message");

    flashes.forEach(flash => {
        setTimeout(() => {
            flash.style.opacity = "0";
            flash.style.transition = "opacity 0.5s ease";

            setTimeout(() => {
                flash.remove();
            }, 300);
        }, 3000); // 3 seconds
    });

    document.querySelectorAll("form").forEach(form => {
        form.addEventListener("submit", (e) => {
            const btn = form.querySelector("button[type='submit']");
            btn.innerHTML = `<span class="flex items-center justify-center gap-2">
                <svg class="animate-spin h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
                Processing...
            </span>`;
            btn.disabled = true;
        });
    });
});
// Inside your DOMContentLoaded listener:

const moneyForm = document.getElementById("moneyForm");
const customInput = document.getElementById("customAmount");
const presetRadios = document.querySelectorAll('.preset-radio');
const finalAmountInput = document.getElementById("finalAmountValue");

// If typing custom, uncheck presets
customInput.addEventListener("input", () => {
    if (customInput.value !== "") {
        presetRadios.forEach(r => r.checked = false);
    }
});

// If clicking preset, clear custom
presetRadios.forEach(radio => {
    radio.addEventListener("change", () => {
        if(radio.checked) customInput.value = "";
    });
});

// Intercept submit to set the final value
moneyForm.addEventListener("submit", function(e) {
    const selectedRadio = document.querySelector('.preset-radio:checked');
    const fridgeSelected = document.getElementById("moneyFridgeValue").value;

    let total = 0;
    if (selectedRadio) {
        total = selectedRadio.value;
    } else if (customInput.value) {
        total = customInput.value;
    }

    if (total <= 0) {
        e.preventDefault();
        alert("Please select or enter a donation amount.");
        return;
    }

    if (!fridgeSelected) {
        e.preventDefault();
        alert("Please select a target fridge.");
        return;
    }

    // Set the hidden field that the Python backend actually reads
    finalAmountInput.value = total;
});
</script>
</body>
</html>