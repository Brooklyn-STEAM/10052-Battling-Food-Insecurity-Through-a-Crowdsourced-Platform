<!DOCTYPE html>
<html lang="en" id="htmlRoot">
<head>
    <meta charset="UTF-8">
    <title>Donate | Community Support</title>
    <link rel="icon" type="image/png" href="/static/images/logo.png">

    <script src="https://cdn.tailwindcss.com"></script>
<script>
  tailwind.config = {
    darkMode: 'class'
  }
</script>

<script>
(function () {
  const saved = localStorage.getItem("theme");

  if (saved === "dark") {
    document.documentElement.classList.add("dark");
  } else if (saved === "light") {
    document.documentElement.classList.remove("dark");
  } else {
    const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
    document.documentElement.classList.toggle("dark", prefersDark);
  }
})();
</script>

    <style>
/* INPUT */
.input {
    width: 100%;
    padding: 0.75rem;
    border-radius: 0.75rem;
    border: 1px solid #d1d5db;
    background: #f8fafc;
    transition: 0.25s;
}
.input:focus {
    border-color: #a78bfa;
    box-shadow: 0 0 0 3px rgba(167,139,250,0.3);
}

/* FORM CARD (purple theme) */
.form-card {
    background: linear-gradient(135deg, #ffffff, #f3f0ff);
    border: 1px solid #e9d5ff;
}
.dark .form-card {
    background: linear-gradient(135deg, #1e1b2e, #2a2545);
    border: 1px solid #40376E;
}

/* DARK MODE */
.dark .input {
    background: #2a2545;
    border-color: #40376E;
    color: #fff;
}

.dark .dropdown {
    background: #2a2545;
    border-color: #40376E;
}
html {
    scroll-behavior: smooth;
}
html {
  transition: background-color 0.3s ease, color 0.3s ease;
}
    </style>
</head>

<body class="bg-white text-gray-800 
             dark:bg-[#1b1730] dark:text-gray-100 
             transition-colors duration-300">{% include "components/navbar.html.jinja" %}

<div class="max-w-5xl mx-auto px-4 py-10">

    <!-- HEADER -->
    <div class="flex justify-between items-center mb-10">
        <div>
            <h1 class="text-4xl font-extrabold text-gray-900 dark:text-white">
                Help People In Need
            </h1>
            <p class="text-gray-600 dark:text-gray-300 mt-2">
                Donate money or food to support local communities.
            </p>
        </div>


    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">

        <!-- 💳 MONEY FORM -->
        <div class="form-card rounded-2xl shadow-lg p-6 space-y-5">

            <h2 class="text-2xl font-bold dark:text-white">
                Donate Money
            </h2>

            <form method="POST" action="{{ url_for('donate_money') }}" id="moneyForm" class="space-y-5">

                <!-- AMOUNTS -->
                <div class="grid grid-cols-3 gap-3 font-semibold">
                    {% for amount in [10, 25, 50] %}
                    <label class="cursor-pointer">
                        <input type="radio" name="amount" value="{{ amount }}" class="hidden peer">
                        <div class="p-4 text-center rounded-xl border
                            peer-checked:bg-purple-600 peer-checked:text-white dark: text-gray-300 peer-checked:border-purple-600 hover:bg-purple-100 dark:hover:bg-[#40376E] transition">
                            ${{ amount }}
                        </div>
                    </label>
                    {% endfor %}
                </div>

                <!-- CUSTOM -->
                <input type="number" name="custom_amount" id="customAmount"
                    placeholder="Custom amount" class="input" min="1" max="10000">

                <!-- 🧊 FRIDGE SELECT (KEEPED) -->
                <div class="relative">
                    <label class="block text-sm text-gray-600 dark:text-gray-300 mb-2">
                        Select a Fridge
                    </label>

                    <input type="hidden" name="FridgeID" id="moneyFridgeValue" required>

                    <button type="button" id="moneyFridgeBtn"
                        class="w-full p-3 rounded-lg border bg-white dark:bg-[#2a2545] dark:text-white flex justify-between">

                        <span id="moneyFridgeText">Choose a fridge</span>
                        <span>▼</span>
                    </button>

                    <div id="moneyFridgeMenu"
                        class="dropdown absolute z-10 w-full mt-2 rounded-lg border hidden">

                        {% for fridge in fridges %}
                        <div class="money-item p-3 cursor-pointer hover:bg-purple-100 dark:hover:bg-[#40376E]"
                            data-id="{{ fridge['ID'] }}"
                            data-name="{{ fridge['Name'] }}">
                            {{ fridge['Name'] }}
                        </div>
                        {% endfor %}
                    </div>
                </div>

                <button class="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-xl">
                    Donate Now
                </button>
            </form>
        </div>

        <!-- 🍱 FOOD FORM -->
        <div class="form-card rounded-2xl shadow-lg p-6 space-y-5">

            <h2 class="text-2xl font-bold dark:text-white">
                Schedule Food Drop-Off
            </h2>

            <form method="POST" action="{{ url_for('donate_food') }}" class="space-y-4">

                <input type="text" name="full_name" placeholder="Full Name" class="input">
                <input type="email" name="food_email" placeholder="Email" class="input">
                <input type="date" name="dropoff_date" class="input">
                <input type="number" name="quantity" placeholder="Quantity" class="input">
                <textarea name="notes" placeholder="Notes (optional)" class="input"></textarea>

                <!-- 🧊 FRIDGE SELECT (KEEPED) -->
                <div class="relative">
                    <label class="block text-sm text-gray-600 dark:text-gray-300 mb-2">
                        Select a Fridge
                    </label>

                    <input type="hidden" name="FridgeID" id="foodFridgeValue" required>

                    <button type="button" id="foodFridgeBtn"
                        class="w-full p-3 rounded-lg border bg-white dark:bg-[#2a2545] dark:text-white flex justify-between">

                        <span id="foodFridgeText">Choose a fridge</span>
                        <span>▼</span>
                    </button>

                    <div id="foodFridgeMenu"
                        class="dropdown absolute z-10 w-full mt-2 rounded-lg border hidden">

                        {% for fridge in fridges %}
                        <div class="food-item p-3 cursor-pointer hover:bg-purple-100 dark:hover:bg-[#40376E]"
                            data-id="{{ fridge['ID'] }}"
                            data-name="{{ fridge['Name'] }}">
                            {{ fridge['Name'] }}
                        </div>
                        {% endfor %}
                    </div>
                </div>

                <button class="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-xl">
                    Donate Now
                </button>
            </form>
        </div>

    </div>
</div>

<!-- 🌙 DARK MODE -->


<!-- 🧊 DROPDOWNS -->
<script>
function setupDropdown(btnId, menuId, itemsClass, inputId, textId) {
    const btn = document.getElementById(btnId);
    const menu = document.getElementById(menuId);
    const input = document.getElementById(inputId);
    const text = document.getElementById(textId);

    if (!btn || !menu) return;

    btn.onclick = () => menu.classList.toggle("hidden");

    document.querySelectorAll("." + itemsClass).forEach(item => {
        item.onclick = () => {
            input.value = item.dataset.id;
            text.textContent = item.dataset.name;
            menu.classList.add("hidden");
        };
    });

    document.addEventListener("click", () => menu.classList.add("hidden"));
}

setupDropdown("moneyFridgeBtn","moneyFridgeMenu","money-item","moneyFridgeValue","moneyFridgeText");
setupDropdown("foodFridgeBtn","foodFridgeMenu","food-item","foodFridgeValue","foodFridgeText");
</script>

<!-- 💰 AMOUNT LOGIC -->
<script>
const customInput = document.getElementById("customAmount");
const presetRadios = document.querySelectorAll('input[name="amount"]');

if (customInput) {
    customInput.addEventListener("input", () => {
        if (customInput.value !== "") {
            presetRadios.forEach(r => r.checked = false);
        }
    });
}

presetRadios.forEach(radio => {
    radio.addEventListener("change", () => {
        if (radio.checked) customInput.value = "";
    });
});
</script>
<script>
if ('scrollRestoration' in history) {
    history.scrollRestoration = 'manual';
}
</script>

</body>
</html>