  <!-- FridgeNet Navbar -->
  <nav class="bg-green-600 text-white w-screen shadow-md top-0">
    <div class="max-w-7xl mx-auto px-6 py-5 flex justify-between items-center">

      <!-- Logo -->
      <a class="text-3xl font-bold " href="/">
        FridgeNet
      </a>

      <!-- Desktop Navigation -->
      <ul class=" z-[9999] hidden md:flex mx-auto font-semibold space-x-12">
        <li><a class="hover:text-yellow-300 transition" href="/map">Map</a></li>
        <li><a class="hover:text-yellow-300 transition" href="/donate">Donate</a></li>
      </ul>
  
      <!-- Desktop Account Dropdown -->
      <div class="hidden xl:flex items-center space-x-5">
        <!-- Account Dropdown -->
    <div class="relative group inline-block"> 
      <button class="flex items-center gap-2 hover:text-gray-200 cursor-pointer focus:outline-none">
      {% if not current_user.is_authenticated %}
         Account
      {% else %}
       Hi {{ current_user.name }}
      {%endif%}
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
        </svg>
      </button>
  <ul class="absolute right-0 mt-2 w-40 bg-green-800 border border-green-700 rounded-md opacity-0 invisible group-hover:opacity-100 
    group-hover:visible transition-all duration-200 text-white ">

  {% if not current_user.is_authenticated %}
      <li><a href="/login" class="block px-4 py-2 hover:bg-green-700">Sign In</a></li>
      <li><a href="/signup" class="block px-4 py-2 hover:bg-green-700">Register</a></li>
   
  {%else%}
    <li><a href="/profile" class="block px-4 py-2 hover:bg-green-700">Profile</a></li>
    <li><a href="/logout" class="block px-4 py-2 hover:bg-green-700">Logout</a></li>
    {%endif%}
      </div>

    </div>

    <!-- Mobile Menu Toggle -->
      <button id="mobile-menu-button" class="xl:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 hover:text-yellow-300 transition" fill="none"
        viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden bg-green-800 text-white md:hidden">
      <ul class="flex flex-col px-6 py-4 space-y-4">
        <li><a class="hover:text-yellow-300 transition" href="/map">Map</a></li>
        <li><a class="hover:text-yellow-300 transition" href="/donate">Donate</a></li>
        <li><a class="hover:text-yellow-300 transition" href="/login">Log In</a></li>
        <li><a class="hover:text-yellow-300 transition" href="/signup">Register</a></li>
      </ul>
    </div>
    
  </nav>

</body>
</html>